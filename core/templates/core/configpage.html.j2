{% extends "core/base.html.j2" %}
{% load static %}
{% block header %}
    {% include 'core/nav.html.j2' with selected='monitors' %}
{% endblock %}
{% block content %}
    <div class="h-[90vh] p-6 pl-[5%] pr-[5%] grid grid-cols-3 gap-4">
        {# Left Panel#}
        <div class="col-span-1 h-full overflow-y-hidden flex flex-col gap-3">
            {# Monitors Header #}
            <div class="flex items-end">
                <span class="text-3xl font-bold">Monitors</span>
            </div>
            <div class="overflow-y-auto flex-grow flex flex-col bg-[#F2F4F8] rounded-xl p-6 gap-4">
                <div class="flex flex-col flex-1">
                    <span class="text-2xl font-bold">Newly Detected</span>
                    <div class="mt-4 flex flex-col gap-3">
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                    </div>
                </div>
                <div class="flex flex-col flex-1">
                    <span class="text-2xl font-bold">Saved</span>
                    <div class="mt-4 flex flex-col gap-3">
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                        <div class="bg-white rounded-xl shadow p-4 text-lg font-bold">Camera Name</div>
                    </div>
                </div>
            </div>
            {# Stop point for `flex-grow` #}
        </div>
        {# Right Panel #}
        <div class="flex flex-col overflow-y-hidden h-full">
            {# Stop point for `flex-grow` #}
        </div>
    </div>
{% endblock content %}
